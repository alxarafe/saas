meta {
  name: Health
  type: http
}

get {
  url: {{base_url}}/health
}

tests {
  test("Status is 200", function() {
    expect(res.getStatus()).to.eql(200);
  });
  test("Body has status ok", function() {
    expect(res.getBody().status).to.eql("ok");
  });
}
